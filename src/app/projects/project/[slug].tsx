import Bounded from '@/components/containers/Bounded';
import styles from './Project.module.css';

export default function Project() {
  return (
    <Bounded className={styles.Project}>
      <h1>Project</h1>
    </Bounded>
  );
}
